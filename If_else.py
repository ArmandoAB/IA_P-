#Corrige el siguiente condicional if else
color = 'rojo'

if color == 'rojo':
    print("El color es rojo.")
else:
    print("El color no es rojo.")

#se reemplaza el 'else' por un 'if' ya que no puede haber un else antes que un if, además que se le agregan comillas a rojo para que detecte que es un string
#se agregan paréntesis para mostrar el texto
#se reemplaza el 'if' por un 'else', ya que no puede haber un else antes que un if, además se elimina la condición del else, ya qeu no lleva condicion
#se agregan paréntesis para mostrar el texto