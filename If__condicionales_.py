#Cambia el operador para que la condición sea True
num1 = 15	#se cran dos numeros con valores diferentes
num2 = 20

if num1 < num2:	#se cambia el operador de 'igual a' a 'menor que'
	print('Se ejecuta el if.');

#Cambia el operador para que la condición sea True
num1 = 1450	#se cran dos numeros con valores diferentes
num2 = 60

if num1 > num2:	#se cambia el poerado de 'menor que' a 'mayor que'
	print('Se ejecuta el if.')

#Haz que el siguiente condicional se convierta en False sin cambiar el operador
num1 = 1450	#se cran dos numeros con valores diferentes
num2 = 6

if num1 == num2:	#se cambia el operador de 'diferenta a' a 'igual a'
	print('Se ejecuta el if.')